export var pageList = {
    'customers':{
        index:'x.html',
        onPageRedir: true, // true = animated open div // false = url redirect to page
        php: false,
        ajax: false,
    },
    'contrats':{
        index:'x.html',
        onPageRedir: true, // true = animated open div // false = url redirect to page
        php: false,
        ajax: false,
    }

}